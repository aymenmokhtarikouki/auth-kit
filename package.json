{
  "name": "auth-kit",
  "version": "0.1.0",
  "private": true,
  "description": "Shared identity toolkit — OTP-first authentication (email/phone codes, Google & Apple sign-in) with pluggable sessions. Storage-agnostic: apps plug small stores (Prisma, raw pg, anything). Address book lives in location-kit.",
  "license": "UNLICENSED",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "setup": "npm install -w @authkit/otp -w @authkit/core -w @authkit/express && npm run build",
    "build": "npm run build -w @authkit/otp -w @authkit/core -w @authkit/express",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "vitest run",
    "demo": "npm run dev -w @authkit/demo-server"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^3.2.0"
  }
}
